services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: mgd_user
      POSTGRES_PASSWORD: mgd_password
      POSTGRES_DB: gestionmgd
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
